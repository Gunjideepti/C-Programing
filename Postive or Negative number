#include<stdio.h>
int main()
{
    int a,b;
    printf("enter a number");
    scanf("%d",&a);
    if(a>0)
    {
        printf("it is a postive number");
    }
    if(a<0)
    {
    printf("it is a negative number");
    }
    if(a==0)
    {
        printf("it is zero");
    }
    return 0;
}
