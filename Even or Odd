#include<stdio.h>
int main()
{
    int a,b;
    printf("enter a number");
    scanf("%d",&a);
    if(a%2==0)
    {
        printf("it is a even number");
    }
    if(a%2!=0)
    {
    printf("it is a odd number");
    }
        return 0;
}
