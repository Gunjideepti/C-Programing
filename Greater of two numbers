#include<stdio.h>
int main()
{
    int a,b;
    printf("enter a number");
    scanf("%d",&a);
    printf("enter a number");
    scanf("%d",&b);
    if(a>b)
    {
        printf("the  %d bigger than %d",a,b);
    }
    if(b>a)
    {
        printf("the %d bigger than %d",b,a);
    }
    return 0;
}
